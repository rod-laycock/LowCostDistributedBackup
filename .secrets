# Secrets file
#
# Complete this file with relevant values, do not commit it back to source control
# 
# All lines start with @ and keys are in uppercase.
# All keys / values are seperated by an = symbol
#
# To use these in a bash script use the following:
#      PASS=$(cat .secret | grep @PASSWORD | cut -d '=' -f 2)  
#
#  .secrets file contains
#  @PASSWORD=MySuperSecretPassword

