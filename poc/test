# Define users
#  Username: user
users:
  - default
  - name: user
    groups:
      - sudo
    sudo: ALL=(ALL) NOPASSWD:ALL
    shell: /bin/bash
    ssh_authorized_keys:
    - VeryLongStringFromPublicKey
